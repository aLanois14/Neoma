@using Neoma.RazorClassLib.Views.Shared
@using Neoma.RazorClassLib.Views.Emails.RefuseProposition
@model RefusePropositionEmailViewModel
@{
    ViewData["EmailTitle"] = "Une proposition a été refusée";
}
<p>
    @Model.CandidatName a refusé votre offre pour le projet <b>@Model.ProjetName</b> pour le motif suivant :
</p>
<p>
    @Model.Motif
</p>
<br />
@await Html.PartialAsync("EmailButton", new EmailButtonViewModel("Consulter détail", Model.EmailUrl))